# 3D Asset Visualizer

金融資産ポートフォリオを3Dで可視化するWebアプリケーションです。

## 使い方

1. **依存関係のインストール:**
   ```
   npm install
   ```

2. **ビルド:**
   ```
   npx esbuild main.js --bundle --outfile=bundle.js
   ```

3. **実行:**
   `index.html` ファイルをブラウザで開いてください。
